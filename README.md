# Fordoun

An example of using Kotlin coroutines with just the standard library, that is, without `kotlinx.coroutines`

## Running tests

`./gradlew --rerun-tasks allTest`

_or_

`docker-compose run --rm app ./gradlew --rerun-tasks allTest`
